---
phase: 01-build-recovery
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/ui/components/AnnotationPreview.tsx
  - src/ui/components/FieldInput.tsx
  - src/ui/components/OverviewRow.tsx
  - src/ui/components/OverviewTab.tsx
  - src/ui/components/SchemaCategory.tsx
  - src/ui/components/SchemaFieldRow.tsx
  - src/ui/components/SchemaTab.tsx
  - src/ui/components/SelectedTab.tsx
  - tests/ui/OverviewRow.test.tsx
autonomous: true
requirements: [BUILD-02, BUILD-05]

must_haves:
  truths:
    - "No React imports remain anywhere in src/ui/"
    - "npm run build completes with zero errors"
    - "All e.target usages have proper type casts"
    - "No schema.required references remain in FieldInput.tsx"
  artifacts:
    - path: "src/ui/components/AnnotationPreview.tsx"
      provides: "Annotation preview component"
      contains: "import { h } from 'preact'"
    - path: "src/ui/components/FieldInput.tsx"
      provides: "Field input component without schema.required"
      contains: "import { h } from 'preact'"
    - path: "src/ui/components/OverviewTab.tsx"
      provides: "Overview tab with preact hooks"
      contains: "import { h } from 'preact'"
    - path: "src/ui/components/SchemaTab.tsx"
      provides: "Schema tab with preact hooks"
      contains: "import { h } from 'preact'"
    - path: "src/ui/components/SelectedTab.tsx"
      provides: "Selected tab with preact hooks"
      contains: "import { h } from 'preact'"
  key_links:
    - from: "src/ui/components/OverviewRow.tsx"
      to: "src/ui/hooks/usePluginMessage.ts"
      via: "import postToPlugin"
      pattern: "from.*usePluginMessage"
    - from: "src/ui/components/SelectedTab.tsx"
      to: "src/ui/hooks/usePluginMessage.ts"
      via: "import postToPlugin"
      pattern: "from.*usePluginMessage"
    - from: "src/ui/components/SchemaTab.tsx"
      to: "src/ui/hooks/usePluginMessage.ts"
      via: "import postToPlugin"
      pattern: "from.*usePluginMessage"
---

<objective>
Fix all 8 UI component files: replace React imports with Preact, cast e.target types, remove schema.required, and fix the test file. After this plan, `npm run build` succeeds with zero errors.

Purpose: Plan 01 fixed the foundation (tsconfig, hook, App.tsx). This plan sweeps every remaining broken component so the build is fully green.
Output: All component files compile, `npm run build` exits 0.
</objective>

<execution_context>
@/Users/sangwon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-recovery/01-RESEARCH.md
@.planning/phases/01-build-recovery/01-01-SUMMARY.md

<interfaces>
<!-- postToPlugin from Plan 01 — components use this to send messages to plugin -->

From src/ui/hooks/usePluginMessage.ts (created in Plan 01):
```typescript
export function postToPlugin<K extends keyof PluginMessageMap>(
  msg: PluginMessageMap[K] extends void
    ? { type: K }
    : { type: K } & PluginMessageMap[K]
): void
```

Components call: `postToPlugin({ type: 'GET_ANNOTATIONS' })` or `postToPlugin({ type: 'SAVE_SCHEMAS', schemas: draft })` — same signature as before, but now backed by cfp emit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix imports and type errors in all 8 component files</name>
  <files>
    src/ui/components/AnnotationPreview.tsx,
    src/ui/components/FieldInput.tsx,
    src/ui/components/OverviewRow.tsx,
    src/ui/components/OverviewTab.tsx,
    src/ui/components/SchemaCategory.tsx,
    src/ui/components/SchemaFieldRow.tsx,
    src/ui/components/SchemaTab.tsx,
    src/ui/components/SelectedTab.tsx
  </files>
  <action>
Apply these changes to each file. Read each file first, then make minimal targeted edits.

**For ALL 8 files — import replacement:**
- Replace `import React from "react"` with `import { h } from 'preact'`
- Replace `import React, { useState, useEffect } from "react"` with `import { h } from 'preact'` + `import { useState, useEffect } from 'preact/hooks'`
- Replace `import React, { useState } from "react"` with `import { h } from 'preact'` + `import { useState } from 'preact/hooks'`
- Only import hooks that are actually used in each file

**File-specific fixes:**

1. **AnnotationPreview.tsx** — Import replacement only (no other issues)

2. **FieldInput.tsx** — Import replacement + remove all 4 `schema.required` references:
   - Remove `{schema.required && <span className="required"> *</span>}` at lines 26, 47, 67, 84
   - Cast `e.target` in onChange handlers: `(e.target as HTMLInputElement).value` (3 occurrences at lines 31, 52, 73)

3. **OverviewRow.tsx** — Import replacement only. Keep `postToPlugin` import from `../hooks/usePluginMessage` (it still exports this function).

4. **OverviewTab.tsx** — Import replacement + hooks from preact/hooks. Cast `e.target` in 2 onChange handlers:
   - Line 145: `(e.target as HTMLSelectElement).value` (category filter)
   - Line 162: `(e.target as HTMLInputElement).value` (search input)

5. **SchemaCategory.tsx** — Import replacement only

6. **SchemaFieldRow.tsx** — Import replacement. Cast all `e.target` usages:
   - `e.target.value` occurrences: cast as `(e.target as HTMLInputElement).value` or `(e.target as HTMLSelectElement).value` depending on element type
   - `e.target.checked` occurrences: cast as `(e.target as HTMLInputElement).checked`
   - Fix the `.map((s) => s.trim())` implicit any — add type: `.map((s: string) => s.trim())`

7. **SchemaTab.tsx** — Import replacement + hooks. Cast `e.target` at line 74: `(e.target as HTMLSelectElement).value`. Keep `postToPlugin` import. Add type annotation for `prev` parameter in any `setDraft(prev => ...)` calls if TypeScript cannot infer it.

8. **SelectedTab.tsx** — Import replacement + hooks. Cast `e.target` at line 98: `(e.target as HTMLSelectElement).value`. Keep `postToPlugin` import.

**What NOT to change:**
- Do NOT replace native HTML elements with cfp UI components (that is Phase 2 scope)
- Do NOT change any component logic, props, or state management
- Do NOT touch `src/ui/components/RawTextEditor.tsx` (already correct)
- Do NOT touch `src/ui/main.tsx` (already correct)
- Do NOT touch files in `src/shared/` or `src/plugin/`
  </action>
  <verify>
    <automated>grep -r 'from "react"' src/ui/ && echo "FAIL: React imports remain" || echo "PASS: No React imports"</automated>
  </verify>
  <done>All 8 component files use preact imports. Zero `from "react"` imports in src/ui/. All schema.required references removed. All e.target usages properly cast.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test file and verify full build</name>
  <files>tests/ui/OverviewRow.test.tsx</files>
  <action>
1. Read `tests/ui/OverviewRow.test.tsx`
2. Remove `onNavigate={vi.fn()}` from ALL render call sites (approximately 8 occurrences). The `OverviewRow` component does not have an `onNavigate` prop — it was removed in commit 9d22d57.
3. Remove the entire test case "clicking the card calls onNavigate" (it tests a prop that does not exist).
4. In the test "clicking Edit button calls onEdit but not onNavigate", remove the `onNavigate` assertion and rename to just "clicking Edit button calls onEdit". Remove `onNavigate` variable.
5. Ensure the test file imports from `'preact'` not `'react'` (check current imports).

After fixing the test file, run the full build:
```bash
npm run build
```

This must exit 0 with zero TypeScript errors. If any errors remain, diagnose and fix them — they should all be in files already touched by this plan.

Then run tests:
```bash
npm run test -- --run
```

Tests should pass. If any test fails due to the `onNavigate` removal, update the test to match the current component interface.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>`npm run build` exits 0 with zero errors. `npm run test -- --run` passes. No onNavigate references in test file. BUILD-05 satisfied.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` exits 0 (BUILD-05)
- `grep -r 'from "react"' src/ui/` returns empty (BUILD-02)
- `grep -r 'schema\.required' src/ui/` returns empty
- `grep 'onNavigate' tests/ui/OverviewRow.test.tsx` returns empty
- `npm run test -- --run` passes
</verification>

<success_criteria>
- Zero `from "react"` imports in src/ui/ (BUILD-02)
- `npm run build` exits 0 with zero TypeScript errors or warnings (BUILD-05)
- All e.target usages have HTMLInputElement/HTMLSelectElement casts
- schema.required removed from FieldInput.tsx (4 occurrences)
- OverviewRow.test.tsx compiles without onNavigate prop errors
- `npm run test -- --run` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-recovery/01-02-SUMMARY.md`
</output>
